{
  "name": "setup-k0s",
  "version": "1.0.0",
  "description": "GitHub Action to install and configure k0s",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "kubernetes",
    "k0s"
  ],
  "author": "fenio",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@vercel/ncc": "^0.38.4",
    "typescript": "^5.9.3"
  }
}
